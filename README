project requires:

    These 3rd party libs:
        pyspotify 2, pyalsaaudio

    And requires these files: (obtainable via a paid spotify account and registering it with spotify-dev)
        spotify_appkey.key, spotify_cred_blob.txt

    to use gui:
        wxpython

    to use vim plugin:
        add project location to PYTHONPATH

execute "run_my_spotify.py" to launch gui
execute "vim/launch_spotify_vim.py" to launch vim plugin
